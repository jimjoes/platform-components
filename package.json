{
  "name": "@jimjoes/platform-packages",
  "version": "1.0.0",
  "main": "index.js",
  "author": "James Hunter",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build": "run-p clean run-s build:api",
    "build:api": "tsc -b ./packages/tsconfig.api-base.json",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "release": "lerna publish"
  },
  "dependencies": {
    "rimraf": "^3.0.1"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}
